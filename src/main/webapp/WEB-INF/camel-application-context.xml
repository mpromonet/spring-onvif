<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:camel="http://camel.apache.org/schema/spring"
	xmlns:cxf="http://camel.apache.org/schema/cxf"
	xmlns:soap="http://cxf.apache.org/bindings/soap"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
  				http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
				http://camel.apache.org/schema/cxf http://camel.apache.org/schema/cxf/camel-cxf.xsd
				http://cxf.apache.org/bindings/soap http://cxf.apache.org/schemas/configuration/soap.xsd">

	<import resource="classpath:META-INF/cxf/cxf.xml" />
	
	<import resource="camel-route-device.xml" />
	<import resource="camel-route-events.xml" />
	<import resource="camel-route-recording.xml" />
	<import resource="camel-route-replay.xml" />
	<import resource="camel-route-ptz.xml" />

	<camelContext id="camelProcess" xmlns="http://camel.apache.org/schema/spring" useMDCLogging="true">
		<jmxAgent id="agent" disabled="true"/>
		
		<!--  devicemgmt.wsdl -->
		<routeContextRef ref="DeviceRoutes" />

		<!--  events.wsdl -->
		<routeContextRef ref="EventRoutes" />

		<!--  events.wsdl -->
		<routeContextRef ref="RecordingRoutes" />

		<!--  replay.wsdl -->
		<routeContextRef ref="ReplayRoutes" />

		<!--  ptz.wsdl -->
		<routeContextRef ref="PTZRoutes" />

		<!--  log -->
		<intercept>
			<to uri="log:interceptor?showHeaders=true&amp;level=INFO" />
		</intercept>
		
		
		<!--  hello -->
		<route>
			<from uri="servlet:///hello" />
			<setBody>
				<constant>Hello</constant>
			</setBody>
		</route>
		
		<!--  probe -->
		<route>
			<from uri="servlet:///probe" />
			<setBody>
				<groovy>
					request = new org.apache.cxf.ws.discovery.wsdl.GetClient();
					request;
				</groovy>
			</setBody>
			<to uri="cxf:/discovery?serviceClass=org.apache.cxf.ws.discovery.WSDiscoveryClient&amp;loggingFeatureEnabled=true&amp;dataFormat=PAYLOAD"/>
		</route>

		<!--  discovery -->
		<route>
			<from uri="cxf:/discovery?serviceClass=org.apache.cxf.ws.discovery.WSDiscoveryService&amp;loggingFeatureEnabled=true&amp;dataFormat=PAYLOAD"/>
			<to uri="log:discovery"/>
		</route>


	</camelContext>

</beans>