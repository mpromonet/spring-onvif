<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:camel="http://camel.apache.org/schema/spring"
	xmlns:camelcxf="http://camel.apache.org/schema/cxf"
	xmlns:cxf="http://cxf.apache.org/core"
	xmlns:soap="http://cxf.apache.org/bindings/soap"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
  				http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
				http://camel.apache.org/schema/cxf http://camel.apache.org/schema/cxf/camel-cxf.xsd
				http://cxf.apache.org/core http://cxf.apache.org/schemas/core.xsd
				http://cxf.apache.org/bindings/soap http://cxf.apache.org/schemas/configuration/soap.xsd">

	<import resource="classpath:META-INF/cxf/cxf.xml" />
    
	<import resource="camel-route-discovery.xml" />
	<import resource="camel-route-device.xml" />
	<import resource="camel-route-events.xml" />
	<import resource="camel-route-recording.xml" />
	<import resource="camel-route-replay.xml" />
	<import resource="camel-route-ptz.xml" />

	<cxf:bus>
		<cxf:features>
			<cxf:logging/>
		</cxf:features>	
		<cxf:properties>
			<entry key="org.apache.cxf.service.ws-discovery.address" value="http://127.0.0.1:9999" />
		</cxf:properties>
	</cxf:bus>

	<camelContext id="camelProcess" xmlns="http://camel.apache.org/schema/spring" useMDCLogging="false">
		<jmxAgent id="agent" disabled="true"/>

		<!--  remotediscovery.wsdl -->
		<routeContextRef ref="DiscoveryRoutes" />

		<!--  devicemgmt.wsdl -->
		<routeContextRef ref="DeviceRoutes" />

		<!--  events.wsdl -->
		<routeContextRef ref="EventRoutes" />

		<!--  events.wsdl -->
		<routeContextRef ref="RecordingRoutes" />

		<!--  replay.wsdl -->
		<routeContextRef ref="ReplayRoutes" />

		<!--  ptz.wsdl -->
		<routeContextRef ref="PTZRoutes" />

		<!--  log -->
		<intercept>
			<to uri="log:interceptor?showHeaders=false&amp;level=INFO" />
		</intercept>
		
		
		<!--  hello -->
		<route>
			<from uri="servlet:///hello" />
			<setBody>
				<constant>Hello</constant>
			</setBody>
		</route>
		
		<!--  probe -->
		<route>
			<from uri="servlet:///probe" />
			<setBody>
				<groovy>
					client = new org.apache.cxf.ws.discovery.WSDiscoveryClient();
					println "###############servlet:///probe wsclient address:" + client.getAddress() + "#############";
					response = client.probe();
					println "############################";
					
					content = "&lt;root&gt;";
					response.each( { content += it } );
					content += "&lt;/root&gt;";
					
					result = "&lt;?xml version=\"1.0\"?&gt;";
					result += content.replaceAll(/&lt;\?xml[^\?]*\?&gt;/,'');
				</groovy>
			</setBody>
		</route>

		<!--  discovery -->
		<route>
			<from uri="cxf:/discovery?serviceClass=org.apache.cxf.ws.discovery.WSDiscoveryService&amp;dataFormat=PAYLOAD"/>
			<to uri="log:discovery"/>
		</route>


	</camelContext>

</beans>